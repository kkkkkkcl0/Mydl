[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "mydl"
version = "0.1.0"
description = "My deep learning learning framework"
requires-python = ">=3.10"
dependencies = [
    "torch",
]